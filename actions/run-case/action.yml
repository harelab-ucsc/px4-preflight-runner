name: Run Preflight Case
description: >
  Executes one resolved test case from the matrix produced by
  parse-spec. Handles ros_unit, px4_build, and sim case types.

inputs:
  case-json:
    description: JSON object for one matrix entry (typically toJson(matrix)).
    required: true
  repo-root:
    description: Path to the checked-out caller repo on disk.
    required: false
    default: ${{ github.workspace }}

runs:
  using: composite
  steps:
    # The runner container ships Python 3.10 + pyyaml + pyulog + jinja2
    # pre-installed (see docker/Dockerfile). We deliberately do NOT call
    # actions/setup-python here: it overlays a hosted-toolcache Python whose
    # pip shebang points at /opt/hostedtoolcache/... — a path that doesn't
    # exist inside our container, breaking every `pip` invocation.

    # Resolve every external ROS repo's version to its actual commit SHA via
    # git ls-remote so that a push to a floating ref (e.g. 'main') busts the
    # cache even when spec.yaml hasn't changed.
    - name: Compute ROS external deps cache key
      id: ros-deps-key
      if: ${{ fromJson(inputs.case-json).type != 'px4_build' }}
      shell: bash
      env:
        CASE_JSON: ${{ inputs.case-json }}
      run: |
        key=$(python3 - <<'PYEOF'
import json, hashlib, os, re, subprocess

case = json.loads(os.environ["CASE_JSON"])
px4_ref = case.get("px4_ref", "main")
ros_distro = case.get("ros_distro", "humble")
extra = case.get("ros_workspace_extra") or {}

def resolve_sha(url, version):
    """Return the commit SHA for a git ref.

    Falls back to the bare version string if the ref is already a full SHA
    or if ls-remote fails (network hiccup, private repo without auth, etc.).
    """
    if re.fullmatch(r"[0-9a-f]{40}", version.lower()):
        return version
    try:
        out = subprocess.check_output(
            ["git", "ls-remote", "--exit-code", url, version],
            stderr=subprocess.DEVNULL,
            timeout=30,
        )
        return out.decode().splitlines()[0].split("\t")[0]
    except Exception:
        return version

# Derive the px4_msgs branch from px4_ref and resolve it to a SHA.
m = re.match(r"^v(\d+)\.(\d+)", px4_ref)
msgs_branch = f"release/{m.group(1)}.{m.group(2)}" if m else "main"
msgs_sha = resolve_sha("https://github.com/PX4/px4_msgs.git", msgs_branch)

# Resolve every ros_workspace_extra entry's version to a SHA.
resolved = {}
for name in sorted(extra):
    entry = dict(extra[name])
    entry["version"] = resolve_sha(entry.get("url", ""), entry.get("version", ""))
    resolved[name] = entry

blob = f"{px4_ref}-{ros_distro}-{msgs_sha}-{json.dumps(resolved, sort_keys=True)}"
print(hashlib.sha256(blob.encode()).hexdigest()[:16])
PYEOF
        )
        echo "key=${key}" >> "${GITHUB_OUTPUT}"

    # Pre-built colcon install for px4_msgs + ros_workspace_extra. The run
    # scripts source this as an underlay and skip rebuilding those packages,
    # saving the 5-7 min px4_msgs compile on every job.
    - name: Restore ROS external deps
      id: ros-ext-cache
      if: ${{ fromJson(inputs.case-json).type != 'px4_build' }}
      uses: actions/cache@v4
      with:
        path: /opt/ros_external_ws
        key: ros-ext-${{ steps.ros-deps-key.outputs.key }}-${{ runner.os }}

    - name: Restore PX4 ccache
      if: ${{ fromJson(inputs.case-json).type != 'ros_unit' }}
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: px4-sih-${{ fromJson(inputs.case-json).px4_ref }}-${{ runner.os }}
        restore-keys: |
          px4-sih-${{ fromJson(inputs.case-json).px4_ref }}-

    # Pre-built PX4 SITL runtime files. run_px4_build.sh checks for bin/px4
    # and skips the full clone+make when the file is present, saving the
    # 10-20 min first-build cost on every sim/px4_build job.
    # Only bin/ and etc/ are cached — intermediate object files are handled
    # by ccache above instead.
    - name: Restore PX4 build
      if: ${{ fromJson(inputs.case-json).type != 'ros_unit' }}
      uses: actions/cache@v4
      with:
        path: |
          /tmp/PX4-Autopilot/build/px4_sitl_default/bin
          /tmp/PX4-Autopilot/build/px4_sitl_default/etc
        key: px4-sitl-${{ fromJson(inputs.case-json).px4_ref }}-${{ runner.os }}

    - name: Restore Micro-XRCE-DDS Agent
      if: ${{ fromJson(inputs.case-json).type == 'sim' }}
      id: xrce-cache
      uses: actions/cache@v4
      with:
        path: /opt/MicroXRCEAgent
        key: xrce-agent-v2.4.3-${{ runner.os }}

    - name: Execute case
      shell: bash
      env:
        CASE_JSON: ${{ inputs.case-json }}
        REPO_ROOT: ${{ inputs.repo-root }}
        ACTION_ROOT: ${{ github.action_path }}
        XRCE_CACHE_HIT: ${{ steps.xrce-cache.outputs.cache-hit }}
      run: bash "${ACTION_ROOT}/../../scripts/run_case.sh"

    - name: Upload artifacts
      # `always()` so artifacts upload on test failure too — that's when
      # they're most useful for debugging.
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: preflight-${{ fromJson(inputs.case-json).name }}-${{ github.run_id }}
        # The runner script's EXIT trap already mirrors /root/.ros/log into
        # /tmp/preflight_output/ros_launch_logs, so a single path is enough.
        path: /tmp/preflight_output/
        # warn rather than ignore so that a silently-empty artifact dir is
        # visible in the summary
        if-no-files-found: warn
        retention-days: 14
